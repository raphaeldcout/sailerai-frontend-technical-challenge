import { FC } from 'react';

import { Typography } from '@/components/Typography';

import { Root } from './style';

export const Header: FC = () => (
  <Root>
    <Typography>Iniciar nova conversa</Typography>
  </Root>
);
